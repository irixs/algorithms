#include <bits/stdc++.h>
#include <math.h>
using namespace std;

int main () {
    int t;
    cin >> t;
    if (t == 1) {
        cout << -1;
    }
    else {
        cout << (int)sqrt(t) + 1 << " " << (int)sqrt(t) + 1;
    }
    return 0;
}